<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<include resource="org/springframework/boot/logging/logback/base.xml"/>

	<jmxConfigurator/>

	<springProperty scope="context" name="springAppName" source="spring.application.name"/>
	<springProperty scope="context" name="logstashDestination" source="logstash.destination"/>
	<springProperty scope="context" name="rockemqNameServerAddress" source="rocketmq.namesrv-addr"/>

	<property name="log_file" value="logs/${springAppName}"/>
	<!--<property name="log_pattern" value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(-&#45;&#45;){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>-->
	<property name="log_pattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} ${LOG_LEVEL_PATTERN:-%5p} ${PID} --- [%t] %logger.%M %L : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

	<contextName>${springAppName}</contextName>

	<appender name="ROLLINGFILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<File>${log_file}.log</File>

		<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<FileNamePattern>${log_file}.%d{yyyy-MM-dd}.%i.log</FileNamePattern>
			<!-- 单个日志文件最多500MB, 30天的日志周期，最大不能超过5GB -->
			<maxFileSize>500MB</maxFileSize>
			<maxHistory>30</maxHistory>
			<totalSizeCap>5GB</totalSizeCap>
		</rollingPolicy>

		<encoder>
			<charset>UTF-8</charset>
			<pattern>${log_pattern}</pattern>
		</encoder>
	</appender>

	<appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
		<destination>${logstashDestination}</destination>
		<encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
			<providers>
				<timestamp>
					<timeZone>GMT+8</timeZone>
				</timestamp>
				<pattern>
					<pattern>
						{
						"severity": "%level",
						"service": "${springAppName:-}",
						"trace": "%X{X-B3-TraceId:-}",
						"span": "%X{X-B3-SpanId:-}",
						"parent": "%X{X-B3-ParentSpanId:-}",
						"exportable": "%X{X-Span-Export:-}",
						"pid": "${PID:-}",
						"thread": "%thread",
						"class": "%logger",
						"method": "%M",
						"linenumber": "%L",
						"rest": "%message"
						}
					</pattern>
				</pattern>
			</providers>
		</encoder>
	</appender>

	<appender name="ROCKETMQ" class="org.apache.rocketmq.logappender.logback.RocketmqLogbackAppender">
		<tag>*</tag>
		<topic>TopicTest</topic>
		<producerGroup>test_transaction_group_1</producerGroup>
		<!--
			<nameServerAddress>${rockemqNameServerAddress}</nameServerAddress>
			The producer group[xxxx] has been created before, specify another name please.
		-->
		<nameServerAddress>192.168.204.129:9876</nameServerAddress>
		<layout>
			<pattern>%date %p %t - %m%n</pattern>
		</layout>
	</appender>

	<appender name="ROCKETMQ_ASYNC" class="ch.qos.logback.classic.AsyncAppender">
		<queueSize>1024</queueSize>
		<discardingThreshold>80</discardingThreshold>
		<maxFlushTime>2000</maxFlushTime>
		<neverBlock>true</neverBlock>
		<appender-ref ref="ROCKETMQ"/>
	</appender>

	<logger name="com.ritoinfo.framework.evo" level="DEBUG" additivity="false">
		<appender-ref ref="CONSOLE"/>
		<appender-ref ref="ROLLINGFILE"/>
		<appender-ref ref="LOGSTASH"/>
		<appender-ref ref="ROCKETMQ_ASYNC"/>
	</logger>

	<logger name="org.apache.rocketmq" level="DEBUG" additivity="false">
		<appender-ref ref="CONSOLE"/>
		<appender-ref ref="ROLLINGFILE"/>
		<appender-ref ref="LOGSTASH"/>
		<appender-ref ref="ROCKETMQ_ASYNC"/>
	</logger>

	<root level="INFO">
		<appender-ref ref="CONSOLE"/>
		<appender-ref ref="ROLLINGFILE"/>
		<appender-ref ref="LOGSTASH"/>
		<appender-ref ref="ROCKETMQ_ASYNC"/>
	</root>

</configuration>
